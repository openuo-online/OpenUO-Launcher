name: Build and Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          # Windows x64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: rust-launcher.exe
            asset_name: OpenUO-Launcher-windows-x64.exe
          
          # macOS ARM64 (Apple Silicon)
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: rust-launcher
            asset_name: OpenUO-Launcher-macos-arm64
          
          # macOS x64 (Intel)
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: rust-launcher
            asset_name: OpenUO-Launcher-macos-x64

    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      
      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
      
      - name: Cache cargo index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}
      
      - name: Cache cargo build
        uses: actions/cache@v4
        with:
          path: target
          key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('**/Cargo.lock') }}
      
      - name: Build
        run: cargo build --release --target ${{ matrix.target }}
      
      - name: Rename artifact
        shell: bash
        run: |
          cp target/${{ matrix.target }}/release/${{ matrix.artifact_name }} ${{ matrix.asset_name }}
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: ${{ matrix.asset_name }}

  release:
    name: Create Rolling Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      
      - name: Display structure of downloaded files
        run: ls -R artifacts/
      
      - name: Delete old release
        uses: dev-drprasad/delete-tag-and-release@v1.1
        with:
          tag_name: latest
          github_token: ${{ secrets.GITHUB_TOKEN }}
          delete_release: true
        continue-on-error: true
      
      - name: Get version from Cargo.toml
        id: get_version
        run: |
          VERSION=$(grep '^version' Cargo.toml | head -1 | cut -d'"' -f2)
          echo "version=v$VERSION" >> $GITHUB_OUTPUT
      
      - name: Create new release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: ${{ steps.get_version.outputs.version }}
          body: |
            🚀 自动构建的最新版本
            
            📦 版本: ${{ steps.get_version.outputs.version }}
            📅 构建时间: ${{ github.event.head_commit.timestamp }}
            📝 提交信息: ${{ github.event.head_commit.message }}
            🔗 提交 SHA: ${{ github.sha }}
            
            ---
            
            ### 下载
            - **Windows x64**: OpenUO-Launcher-windows-x64.exe
            - **macOS ARM64** (Apple Silicon): OpenUO-Launcher-macos-arm64
            - **macOS x64** (Intel): OpenUO-Launcher-macos-x64
          files: |
            artifacts/OpenUO-Launcher-windows-x64.exe/OpenUO-Launcher-windows-x64.exe
            artifacts/OpenUO-Launcher-macos-arm64/OpenUO-Launcher-macos-arm64
            artifacts/OpenUO-Launcher-macos-x64/OpenUO-Launcher-macos-x64
          prerelease: false
          make_latest: true
